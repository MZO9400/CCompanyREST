apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: c-company-rest-deployment
  name: c-company-rest-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: c-company-rest-deployment
  template:
    metadata:
      labels:
        app: c-company-rest-deployment
    spec:
      containers:
        - image: ghcr.io/mzo9400/ccompanyrest:master
          name: c-company-rest-deployment
          envFrom:
            - secretRef:
                name: ccompany-secrets
          ports:
            - containerPort: 3000
              protocol: TCP
      dnsPolicy: ClusterFirst
      restartPolicy: Always


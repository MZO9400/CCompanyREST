apiVersion: v1
kind: Service
metadata:
  name: c-company-rest-node
  labels:
    app: c-company-rest-node
spec:
  type: LoadBalancer
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: c-company-rest-deployment
